# Programmer Guide

The app dynamically loads content from topics.json using JavaScript fetch().

To add a new topic:
1. Add image to images folder
2. Add audio to audio folder
3. Add JSON object inside topics.json

The app uses:
- HTML
- CSS
- JavaScript
- Bootstrap
- Service Workers